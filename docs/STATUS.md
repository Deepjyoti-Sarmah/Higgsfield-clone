# STATUS: the live truth

A line is valid only if it has a **path** and a **verify command** (or a named manual check) with a date.
Keep it short: replace lines as things change instead of piling new ones up. History goes in `WORKLOG.md`.

## WORKS
| What | Where | Verified by | When (UTC) |
|---|---|---|---|
| Library row selection is unmistakable (accent left edge + lighter surface + "Viewing" chip, distinct from hover) and clicking a row scrolls the result into view with focus + `aria-live` announcement (reuses the existing `usePrefersReducedMotion`, never scrolls on first mount even with `?job=` in the URL); video thumbnails get a play glyph, image thumbnails don't | `apps/web/src/features/library/{LibraryItem,LibraryPage,LibraryResultView,libraryCopy}.tsx\|ts` | lint + 60 vitest + tsc -b + build + `scripts/check-standards` all pass; local Playwright screenshots of hover vs selected vs reduced-motion states with real completed jobs (`docs/verification/T-040/`); Create-video's session-history strip has the same weak-selection issue, flagged not fixed (out of scope) | 2026-09-14 |
| Footer sits at the bottom of the viewport on short pages (was floating with dead space below); Library + image-result thumbnails standardised on `aspect-square` (video posters no longer side-cropped, image/video Library rows look consistent) | `apps/web/src/ui/AppShell.tsx`, `apps/web/src/features/library/LibraryItem.tsx`, `apps/web/src/features/image-create/ImageResultGrid.tsx` | lint + 60 vitest + tsc -b + build + `scripts/check-standards` all pass; 390px no-horizontal-scroll on all 5 routes; **deployed live and evidence-checked** (`6ab456a`; live bundle `index-Zg1gDfWe.js` matches a from-scratch worktree build, live CSS contains `.w-36`) after an earlier deploy-verification failure — see the WORKLOG entry below; live screenshots confirm the footer sits flush at the bottom with no void, even in the signed-out error state (`docs/verification/T-039/live-{library,credits}-1440x900.png`, overwritten) | 2026-09-14 |
| Image jobs get real progress + reach the Library: fixed the frontend `onStatus` no-op that left the page frozen on a static "Queued" for the whole ~50-200s FLUX wait (the real bug — the SSE/notify backend plumbing was already correct, confirmed by a raw `curl -N` watch); Library is now kind-agnostic (`LibraryItemResponse` +`kind`/+`prompt`/+`image_urls`, contract change, `openapi.json` diff is only that shape); image Library items show their own grid + the prompt as the label | `apps/web/src/features/image-create/{ImageProgressView,useImageJobProgress,CreateImagePage,ImageStage}.tsx\|ts`, `apps/web/src/api/{useJobEvents,useElapsedSeconds}.ts`, `apps/web/src/features/library/*`, `apps/api/app/{repositories/jobs,services/job_views,schemas/jobs,routers/jobs}.py` | 220 pytest + ruff + mypy + 60 vitest + lint + tsc -b + build + `scripts/check-standards` all pass; local repro against a fake 55s-slow backend: elapsed timer ticks live throughout, resolves to the result with no refresh, then the job appears in `/library` with a real image (full detail in `docs/tasks/T-038/report.md`); **deployed live and evidence-checked** (`6ab456a`; live `/openapi.json` confirms `LibraryItemResponse` now has `kind`/`image_urls`/`prompt` — the contract change is genuinely live). Every deploy attempt between `dedcfdb` and `a4441b9` had actually **failed the build** and been silently serving the pre-T-038 image the whole time — see the WORKLOG correction entry. The one real paid image job on the live site is still **UNVERIFIED**: this IP's 5/5 daily guest cap remains exhausted; not bypassed | 2026-09-14 |
| Fixed a false model claim on Create image (`GPT IMAGE 2 · 4K` -> `FLUX.1-SCHNELL · OUR GPU`; we generate with FLUX.1-schnell on our own Modal H100, never GPT Image 2) and the stale showcase samples (`/showcase/sample-0N.jpg` were still T-030's synthetic gradients; copied over the real FLUX stills, retitled to what each image shows, dropped the wrong "STILL"/"MOTION PREVIEW" copy); grepped `apps/web/src` for GPT/Seedance/Nano Banana/Veo/Kling/Sora/Seedream/Higgsfield Soul/Midjourney/DALL-E/Stable Diffusion/Runway/Luma/Pika — no other hits | `apps/web/src/features/image-create/ImageStage.tsx`, `apps/web/src/api/webMedia.ts`, `apps/web/public/showcase/sample-0{1,2,3,4}.jpg` | lint + 60 vitest + tsc -b + build + `scripts/check-standards` all pass; deployed from a clean detached worktree, `GET /api/health/deep` -> `status: "ok"`; live `/create/image` screenshot confirms `FLUX.1-SCHNELL · OUR GPU` and 4 real 1024x1024 images (Neon Alley/Desert Canyon/Portrait/Forest Path) | 2026-09-14 |
| Explore gallery shows REAL imagery: the 12 preview clips are now built from 4 of our own FLUX.1-schnell stills (our deployed Modal endpoint, licence-clean, not stock/third-party) instead of T-030's synthetic ffmpeg gradients — same `previews/<slug>.mp4`/`.jpg` object keys, no code/contract change | `apps/api/app/adapters/fixtures/preview_sources/source-0{1,2,3,4}.jpg`, `docs/research/preview-sources.md`, `docs/verification/T-036/live-explore.png` | `scripts/build-preset-previews` re-run against live R2 (12/12 uploaded); live `GET /api/v1/presets` -> same URLs; `curl` on 3 sampled clips+posters -> 200 `video/mp4`/`image/jpeg` at real (much larger) byte sizes; live Playwright at 1440x900 shows genuine imagery (neon alley, canyon, portrait, forest) with visible camera motion (two video-element screenshots 2.5s apart show a real dolly) | 2026-09-14 |
| Explore gallery matches the reference (masonry, bare tiles, hover reveal, header CTA), confirmed on the LIVE deployed site with the real R2 preview clips (not local-MinIO stand-ins) | `apps/web/src/features/explore/PresetGallery{,Card}.tsx` | web verify green + T-036 screenshots (`docs/tasks/T-036/report.md`); live Playwright at 1440x900: 12 `<video>` elements all `readyState=4`, `currentTime` advancing frame-to-frame, correct `previews/<slug>.mp4` R2 URLs (no query string, so the presigned-detection fix is untriggered in prod) | 2026-09-14 |
| Deploy with poster fix + both backends live: api+worker deployed from a clean detached worktree (`git worktree add --detach` at `main`@`b71035e`, `railway up --path-as-root`) so the other agent's uncommitted `imageJobs.ts`/`imageJobHelpers.ts` never shipped | Railway `api`+`worker` | `GET /api/health/deep` -> `status: "ok"`, all 5 checks ok, `video_backend: "modal"`, `image_backend: "modal"` | 2026-09-14 |
| Preset preview poster fix: `<video>` tags now set `poster` (mp4 key -> `.jpg`, same R2 object T-030 already uploads) + `preload="metadata"`, so tiles show a still frame instead of black before the clip buffers; shared `previewPosterUrl` helper (one module, used by both cards) | `apps/web/src/api/webMedia.ts`, `apps/web/src/features/explore/PresetGalleryCard.tsx`, `apps/web/src/features/create-video/PresetCard.tsx` | lint + `tsc -b` + 60 vitest + build + `scripts/check-standards` all pass; `curl` confirms `previews/dolly-in.jpg` live 200 `image/jpeg` on R2 | 2026-09-14 |
| Real text→image backend LIVE: `apps/gpu/flux_image.py` deployed as a persistent Modal endpoint (`modal deploy`, H100, `higgsfield-flux-image` app), Railway `api`+`worker` flipped to `IMAGE_GENERATION_BACKEND=modal` with `MODAL_IMAGE_ENDPOINT_URL` set, reusing the existing shared `MODAL_WEBHOOK_SECRET`; api+worker redeployed from a clean detached worktree (`git worktree add --detach`) at `main`@`98f9aa8` so another agent's in-flight, uncommitted `apps/web/src/api/imageJobs.ts`/`imageJobHelpers.ts` never shipped | `apps/gpu/flux_image.py`, `apps/api/app/adapters/{modal_image_adapter,backend_selection}.py`, Railway vars `api`/`worker` | `GET /api/health/deep` -> `image_backend: "modal"` (was `placeholder`); direct probe of the deployed endpoint with the shared bearer secret -> 200, real distinct 512² PNG decoded (magic bytes verified, not a placeholder); did not run a job through the app itself (this IP's per-IP guest cap from T-033 was already exhausted today, so no guest slot was burned to avoid working around the guardrail) | 2026-09-14 (see WORKLOG) |
| Real text→image backend: `ModalImageAdapter` (FLUX.1-schnell, H100) behind the untouched `ImageModelAdapter` protocol; independent `IMAGE_GENERATION_BACKEND`; placeholder fallback relabels itself so it is never shown as real; `ImageResultGrid` shows the badge/notice; **paid probe PASS — 4 real, distinct 1024² images** | `apps/gpu/flux_image.py`, `apps/api/app/adapters/{modal_image_adapter,backend_selection}.py`, `apps/api/app/domain/image_dimensions.py`, `apps/web/src/features/image-create/ImageResultGrid.tsx`, `docs/verification/flux/` | ruff + mypy + `pytest -q` -> 205 passed, `openapi.json` byte-identical, web 60 tests + build, `check-standards` ok; paid probe cold 135.0 s load + 26.3 s gen (~$0.2), warm 10.2 s + 25.7 s in 61.7 s wall (~$0.05) at $3.95/h H100 | 2026-09-14 05:30 |
| Guardrails + real-AI cutover: per-IP guest cap (5/day, hashed IP), per-user 24h job cap (10), daily paid-budget guard (never calls the GPU when exhausted), top-up cap (2/day), typed 429 bodies; `GENERATION_BACKEND=modal` with automatic `local-motion` fallback; `generated_by` on job + library with an "AI video"/"Motion preview" badge | `apps/api/app/services/guardrails.py`, `adapter_runs.py`, `repositories/rate_limits.py`, `migrations/versions/0006_guardrails.py`, `apps/web/src/ui/GenerationBadge.tsx`, `packages/contracts/openapi.json` | `ruff + mypy + pytest -q` -> 191 passed (8 new), `export-openapi` +24 lines with only `generated_by`, web lint/test(60)/build, `check-standards` ok (full output in `docs/tasks/T-033/report.md`); **deployed live** (api + worker at `76b3d34`) with `GENERATION_BACKEND=modal`, `MODAL_ENDPOINT_URL` + `MODAL_WEBHOOK_SECRET`; worker crash on missing `httpx` fixed by promoting it to main deps; **live real-AI run** job `33985fad-…` → `generated_by="modal"` in 132 s with the AI VIDEO badge (`docs/verification/create-video-ai.png`) | 2026-09-14 04:55 |
| Live browser walkthrough (T-011): signed-out Explore (12 tiles, own R2 clips) → guest → real upload + clip job `fa22e5fa-…` → Library row → Share `/v/{id}` 200 with OG tags → credits top-up → create image — **7/7 PASS** | `docs/verification/VERIFICATION-REPORT.md`, `docs/verification/*.png` | Playwright-core + Chromium (headless, 1440x900) against https://api-production-8afc.up.railway.app at commit `4b06a17`; screenshots `explore`, `create-video`, `library`, `share`, `credits`, `create-image` | 2026-09-14 04:55 |
| Explore density pass: full-width 5-up media-first gallery (whole tile is the link, name/category overlay, "Recreate" on hover+focus), compact hero, tool cards reduced to one text row, slimmer shell | `apps/web/src/features/explore/*`, `apps/web/src/ui/AppShell.tsx`, `apps/web/src/styles.css`, `apps/web/src/features/create-video/CreateVideoCanvas.tsx` | Playwright at 1440x900 -> `tileCountAboveFold: 8`, grid 1152px / 224px tiles; lint + tsc + 60 vitest + build + `scripts/check-standards` ok; before/after screenshots in `docs/verification/T-031/` (full output in `docs/tasks/T-031/report.md`) | 2026-09-14 04:35 |
| Preset preview media is ours: 12 ffmpeg-built 720p h264 clips (+posters) in R2 at `previews/<slug>.mp4`; migration `0005` seeds `preview_key`; `GET /api/v1/presets` builds `preview_url` with `build_asset_url`; no hotlinked higgsfield.ai/cloudfront media in the tree | `scripts/build-preset-previews`, `apps/api/app/adapters/preset_preview_build.py`, `apps/api/app/{domain/preset_catalog,services/presets,routers/presets}.py`, `apps/api/migrations/versions/0005_preset_preview_keys.py`, `apps/web/src/api/webMedia.ts` | `scripts/build-preset-previews --dry-run` (12 x 720p h264, 33 s), `uv --directory apps/api run pytest -q` -> 183 passed, local `GET /api/v1/presets` -> 12/12 non-null on `pub-*.r2.dev` + `curl` 200 `video/mp4`, `scripts/check-standards` ok; **live** `GET /api/v1/presets` -> 12/12 non-null on `pub-e14a8ad582a945a7a46dd46e2b138ec2.r2.dev` after deploy `c0640b7` (full output in `docs/tasks/T-030/report.md`) | 2026-09-14 04:20 |
| Prompt/response capture (Claude Code main session) | `.claude/hooks/capture.py`, `.claude/settings.json` | canary prompts in 2 sessions, see `CAPTURE-TEST.md` | 2026-09-12 23:44 |
| Stop-hook race fix (final text flushed after hook fires) | `.claude/hooks/capture.py` | replayed truncated transcript + late append, full text captured | 2026-09-12 23:45 |
| Subagent capture: delegation prompt (`DELEGATE`) + output (`SUBAGENT_RESPONSE`), tagged with the subagent's model | `.claude/hooks/capture.py` (`delegate`, `subagent`), `.claude/settings.json` | live: Haiku canary subagent logged in `.agent-logs/*ff164378*.md`; offline: synthetic transcript tests | 2026-09-13 01:23 |
| `agent-run` wrapper logs the prompt + response of a non-Claude CLI (including errors and timeouts) | `scripts/agent-run` | `scripts/agent-run codex T-000-4` → `.agent-logs/2026-09-13_01-19-16_codex_T-000-4.md` | 2026-09-13 01:24 |
| API skeleton: `GET /api/health` (200/503), `POST /api/v1/auth/guest` (httpOnly cookie), `GET /api/v1/me` (401 without/tampered cookie), worker heartbeat, migration `0001` | `apps/api/`, `packages/contracts/openapi.json` | `docker compose up -d db && uv --directory apps/api run alembic upgrade head && uv --directory apps/api run pytest -q` → 7 passed; ruff + mypy strict clean | 2026-09-13 02:05 (local only, not deployed) |
| Web shell: dark AppShell with 5-item nav, guest button (idle/loading/error), typed client from openapi.json | `apps/web/` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run build` all pass (re-run by the reviewer) | 2026-09-13 02:40 (local) |
| Container image: web build → Python; `APP_ROLE=api` runs migrations + uvicorn and serves the SPA with deep links; `APP_ROLE=worker` logs a heartbeat | `Dockerfile`, `apps/api/entrypoint.sh`, `.dockerignore`, `railway.json` | `docker build -t hf-clone .`; `docker run --network host -e DATABASE_URL=postgresql://…localhost:5432/higgsfield hf-clone` → health 200, `/` + `/create/video` 200, guest 201, me 200; worker run → 2 heartbeats in 12s | 2026-09-13 01:50 (local) |
| Standards check (file ≤200 lines, comment block ≤3 lines) | `scripts/check-standards` | passes on the repo; a planted 205-line file with a 4-line comment fails with exit 1 | 2026-09-13 01:18 |
| Home "Create video" button uses `navigate("/create/video")` instead of nesting a `<button>` in a `<Link>` (valid HTML, one focus stop) | `apps/web/src/features/home/HomePage.tsx` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run build && scripts/check-standards` all pass; `grep -n "<Link"` on the file prints nothing | 2026-09-13 01:55 |
| Spec 003 contract published: `/api/v1/presets`, `/uploads`, `/uploads/{asset_id}/complete`, `/jobs`, `/jobs/{job_id}`, `/jobs/{job_id}/events` (SSE), `/credits` with final schemas; handlers return **501** until T-003-3/4 | `apps/api/app/schemas/{presets,uploads,jobs,credits}.py`, `apps/api/app/routers/{presets,uploads,jobs,credits}.py`, `packages/contracts/openapi.json` | `uv --directory apps/api run ruff check . && uv --directory apps/api run pytest -q && scripts/export-openapi && scripts/check-standards` → 7 passed, 10 paths listed | 2026-09-13 02:18 (local) |
| Spec 003 design + 7 parallel-safe task briefs (77 files, no overlap); 12 ffmpeg motion recipes rendered on ffmpeg 7.1.4 (5s, 120 frames, h264, faststart, no black corners) | `docs/specs/003-generation-core/{design,tasks}.md`, `docs/tasks/T-003-{1..7}/brief.md` | tasks.md file-overlap script + recipe renders, both in `docs/tasks/T-003-0/report.md` | 2026-09-13 02:18 |
| Spec 004 design + 5 parallel-safe web task briefs (61 files, no overlap); every API path in the design exists in `openapi.json`; `vitest@^5` peer range re-checked against the installed `vite@^8` | `docs/specs/004-create-video/{design,tasks}.md`, `docs/tasks/T-004-{1..5}/brief.md` | `python3 -c openapi-paths && ls docs/tasks (filter T-004) && scripts/check-standards` → 10 paths, 5 briefs, 0 violations (full output in `docs/tasks/T-004-0/report.md`) | 2026-09-13 02:35 |
| Generation data foundation: `asset`/`preset`/`job`/`job_step`/`ledger_entry` tables with the 6 required named indexes/checks, migrations `0002`/`0003` (12 presets seeded), domain rules + preset catalog, `ObjectStorage`/`ModelAdapter` Protocols, `S3ObjectStorage`, MinIO + `minio-init` compose services, shared conftest fixtures | `apps/api/app/models/*`, `apps/api/migrations/versions/000{2,3}_*.py`, `apps/api/app/{domain,adapters}/`, `apps/api/app/storage_dependencies.py`, `docker-compose.yml`, `apps/api/tests/conftest.py` | `docker compose up -d --wait db minio && docker compose run --rm minio-init && uv --directory apps/api run alembic upgrade head && uv --directory apps/api run ruff check . && uv --directory apps/api run mypy && uv --directory apps/api run pytest -q && scripts/export-openapi && git diff --exit-code packages/contracts/openapi.json && scripts/check-standards` → 19 passed, mypy clean (12 files), contract byte-identical, 0 violations (re-run by the orchestrator) | 2026-09-13 02:42 |
| Generation repositories: presets, assets, ledger, jobs (+ `pg_notify`), job steps with `FOR UPDATE SKIP LOCKED` claim, lease renew/finish, reaper query, `lock_user_row` | `apps/api/app/repositories/*` | `docker compose up -d --wait db && uv --directory apps/api run alembic upgrade head && uv --directory apps/api run ruff check . && uv --directory apps/api run mypy && uv --directory apps/api run pytest -q && scripts/check-standards` → 29 passed, mypy clean, 0 violations (re-run by the orchestrator) | 2026-09-13 02:45 |
| Create-video web foundation: 24 shared types re-exported from the regenerated typed client, copy tables verbatim from the design, `canvasPhase`/`imageFileRules`/`elapsedTime`/`presetMotionHints`/`usePrefersReducedMotion`, `ui/{buttonStyles,ButtonLink,ProgressBar,Toast}`, vitest runner | `apps/web/src/features/create-video/*.ts`, `apps/web/src/ui/*`, `apps/web/src/styles.css`, `apps/web/package.json`, `apps/web/src/api/generated/schema.d.ts` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run test && npm --prefix apps/web run build && scripts/check-standards` → 24 tests pass, build ok, 0 violations (re-run by the orchestrator) | 2026-09-13 02:47 |
| Presets/uploads/credits endpoints live: `GET /api/v1/presets` (≥12), presigned `POST /uploads` + `complete` (404/409/422 rules), `GET /credits`; a new guest gets a one-time 60-credit GRANT in the same transaction as the user insert | `apps/api/app/services/{presets,uploads,credits,guest_accounts}.py`, `apps/api/app/routers/{presets,uploads,credits}.py` | `docker compose up -d --wait db && uv --directory apps/api run alembic upgrade head && uv --directory apps/api run ruff check . && uv --directory apps/api run mypy && uv --directory apps/api run pytest -q && scripts/export-openapi && git diff --exit-code packages/contracts/openapi.json && scripts/check-standards` → 70 passed, mypy clean (21 files), contract byte-identical, 0 violations (re-run by the orchestrator) | 2026-09-13 02:50 |
| Create-video data hooks: guest-session runner (one shared in-flight promise, one 401 retry), presets + `?preset=` selection, credits (`CreditsControls`), 6-entry session-history store (`SessionHistory`), and the presign → PUT (progress) → complete image upload | `apps/web/src/features/create-video/{useGuestSessionRunner,usePresets,usePresetSelection,useCredits,sessionHistoryStore,useSessionHistory,putFileWithProgress,useImageUpload}.ts` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run test && npm --prefix apps/web run build && scripts/check-standards` → 34 tests pass, build ok, 0 violations (re-run by the orchestrator) | 2026-09-13 02:52 |
| Generation backends: `local-motion` renders a real 5 s ≤720p h264 faststart mp4 + poster for all 12 presets; `mock` copies fixtures instantly; `modal`/`openrouter` fail with "not configured"; `GENERATION_BACKEND` selection | `apps/api/app/adapters/{motion_recipes,local_motion_adapter,mock_model_adapter,modal_adapter,openrouter_adapter,backend_selection}.py`, `apps/api/app/adapters/fixtures/` | `uv --directory apps/api run ruff check . && uv --directory apps/api run mypy && uv --directory apps/api run pytest -q tests/test_local_motion_adapter.py tests/test_backend_selection.py && docker build -t hf-clone . && docker run --rm --entrypoint ffmpeg hf-clone -version && scripts/check-standards` → 26 passed, mypy clean, ffmpeg 7.1.5 in image, 0 violations (full output in `docs/tasks/T-003-6/report.md`) | 2026-09-13 02:56 |
| Jobs API live: `POST /api/v1/jobs` (one transaction: lock → idempotency → preset/asset → balance → job + step + HOLD → `pg_notify`; 202/402/404/409), `GET /jobs/{id}` (owned, presigned URLs), `GET /jobs/{id}/events` SSE (one LISTEN connection per process, per-job queues, 20s ping, close after terminal) | `apps/api/app/services/{job_creation,job_views,job_event_broker,job_event_stream}.py`, `apps/api/app/job_event_dependencies.py`, `apps/api/app/routers/jobs.py`, `apps/api/app/main.py` | `docker compose up -d --wait db && uv --directory apps/api run alembic upgrade head && uv --directory apps/api run ruff check . && uv --directory apps/api run mypy && uv --directory apps/api run pytest -q && scripts/export-openapi && git diff --exit-code packages/contracts/openapi.json && scripts/check-standards` → 98 passed, mypy clean (25 files), contract byte-identical, 0 violations (re-run by the orchestrator) | 2026-09-13 03:00 |
| Worker live: `APP_ROLE=worker` claims one queued step (`FOR UPDATE SKIP LOCKED`), renews the 5-min lease, runs the `GENERATION_BACKEND` adapter, stores mp4 + poster, SETTLEs on success and RELEASEs on failure; the reaper re-queues one expired attempt and then fails + refunds | `apps/api/app/worker.py`, `apps/api/app/services/{step_claiming,generation_runs,step_completion,lease_reaper}.py` | `docker compose up -d --wait db && uv --directory apps/api run alembic upgrade head && uv --directory apps/api run ruff check . && uv --directory apps/api run mypy && uv --directory apps/api run pytest -q && (cd apps/api && GENERATION_BACKEND=mock timeout 6 uv run python -m app.worker; test $? -eq 124) && scripts/check-standards` → 108 passed, mypy clean (29 files), worker looped 6s (exit 124), 0 violations (re-run by the orchestrator) | 2026-09-13 03:06 |
| Create-video panel components: drop / browse / paste image input with inline errors, thumbnail + Replace/Remove, always-visible preset cards with category chips and a selected state, optional prompt + counter, Generate section (cost / balance / Get credits) | `apps/web/src/features/create-video/{CreateVideoPanel,ImageDropZone,ImageThumbnail,PresetPicker,PresetCategoryChips,PresetCard,PromptField,GenerateSection}.tsx`, `apps/web/src/features/create-video/useClipboardImagePaste.ts` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run build && scripts/check-standards` → build ok, 0 violations (re-run by the orchestrator) | 2026-09-13 03:02 |
| Create-video job hooks: idempotent submit (fresh `crypto.randomUUID` key per click, 402/404/409/401 mapping), framework-free status watcher (SSE + 5 s poll fallback, reconnect ladder, terminal close) with 9 unit tests, `useJobEvents`, `useElapsedSeconds`, and `useActiveJob` (history + credits sync, make-another, retry, open history) | `apps/web/src/features/create-video/{useCreateJob,jobStatusWatcher,useJobEvents,useElapsedSeconds,useActiveJob}.ts` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run test && npm --prefix apps/web run build && scripts/check-standards` → 43 tests pass, build ok, 0 violations (re-run by the orchestrator) | 2026-09-13 03:08 |
| Create-video page assembled: `/create/video` (panel + canvas + session-history strip + toast + polite live region), canvas states empty → uploading → ready → submitting → queued → generating → succeeded/failed, result actions (Download / Make another / Share), `/v/:jobId` placeholder route | `apps/web/src/features/create-video/*`, `apps/web/src/App.tsx` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run build && scripts/check-standards` → 78 modules built, 43 tests, 0 violations (re-run by the orchestrator). The in-browser AC pass was NOT run (no browser in this CLI session) — left to `verify-slice` | 2026-09-13 03:32 |
| AC-1 presets + AC-7 guest grant/credits: `GET /presets` = 12 × `credit_cost` 20 signed out; new guest = 60; create holds 20 (bal 40); 3 videos → 0 | `apps/api/app/routers/{presets,credits}.py`, `apps/api/app/services/{presets,credits,guest_accounts}.py` | `scripts/smoke-generation --base-url http://localhost:8000` steps 1, 2, 5, 8 on local compose (uvicorn + worker, `GENERATION_BACKEND=local-motion`) → 9/9 `ok`, exit 0 (full output in `docs/tasks/T-003-7/report.md`) | 2026-09-13 03:27 |
| AC-2 uploads: presigned PUT for jpeg/png/webp ≤10 MB, `complete` marks `ready`; other type/size → 422 (live probes) | `apps/api/app/routers/uploads.py`, `apps/api/app/services/uploads.py` | `scripts/smoke-generation --base-url http://localhost:8000` step 4; `uv --directory apps/api run pytest -q` → `tests/test_uploads_api.py` (108 passed overall) | 2026-09-13 03:27 |
| AC-3 create job: 202 `{queued, 20}` in one transaction, same `idempotency_key` → same `id`, no second HOLD; insufficient → 402 top-level `{detail,balance,required}` | `apps/api/app/routers/jobs.py`, `apps/api/app/services/job_creation.py` | `scripts/smoke-generation --base-url http://localhost:8000` steps 5, 8 (402 `balance=0 required=20`); `uv --directory apps/api run pytest -q` → `tests/test_job_creation_api.py`, `tests/test_job_creation_idempotency_api.py` | 2026-09-13 03:27 |
| AC-4 worker end to end: claims the step, runs `local-motion`, stores mp4 + poster, SETTLEs (ledger `GRANT 60, HOLD −20, SETTLE 0`) | `apps/api/app/worker.py`, `apps/api/app/services/{step_claiming,generation_runs,step_completion}.py`, `apps/api/app/adapters/local_motion_adapter.py` | `scripts/smoke-generation --base-url http://localhost:8000` steps 6–7 (ffprobe: h264 1280×720 120f 5.000 s, faststart) + `/tmp/worker.log` claim/finish per job | 2026-09-13 03:27 |
| AC-5 reaper: an expired lease is re-queued once then failed + refunded; a terminal job's orphan step is failed, not re-claimed forever | `apps/api/app/services/{lease_reaper,step_claiming}.py` | worker startup on the local stack reaped 2 expired leases (`/tmp/worker.log`); `uv --directory apps/api run pytest -q` → `tests/test_lease_reaper.py` | 2026-09-13 03:27 |
| AC-6 progress: SSE `queued → running → succeeded` then close (live wire trace 0.012/0.808/2.095 s), `Cache-Control: no-cache`, `X-Accel-Buffering: no`; non-owner `/jobs/{id}` and `/events` → 404 | `apps/api/app/routers/jobs.py`, `apps/api/app/services/{job_event_stream,job_event_broker}.py` | `scripts/smoke-generation --base-url http://localhost:8000` steps 6, 9; `uv --directory apps/api run pytest -q` → `tests/test_job_events_api.py`, `tests/test_job_reading_api.py` (live 20 s `: ping` is unit-only — see the T-003-7 report) | 2026-09-13 03:27 |
| AC-8 local-motion backend: real 5 s ≤720p h264 faststart mp4 + poster from the input image, per preset recipe | `apps/api/app/adapters/{local_motion_adapter,motion_recipes}.py` | `scripts/smoke-generation --base-url http://localhost:8000` step 7 + atom-order probe (`ftyp, moov, free, mdat`); `uv --directory apps/api run pytest -q` → `tests/test_local_motion_adapter.py`, `tests/test_backend_selection.py` | 2026-09-13 03:27 |
| Independent review of spec 003 (different model: Claude Opus 5): smoke 9/9 PASS on a fresh api+worker, `tsc -b` clean, 107/108 pytest (1 flaky, see BROKEN) | `scripts/smoke-generation` | `scripts/smoke-generation --base-url http://localhost:8021` → PASS (9/9) | 2026-09-13 13:00 |
| Spec 003 end-to-end smoke on local compose (guest → upload → job → SSE → mp4 → credits → owner-only) | `scripts/smoke-generation` | `scripts/smoke-generation --base-url http://localhost:8000` → 9/9 `ok`, exit 0, three runs; chain `docker compose up -d --wait db minio && docker compose run --rm minio-init && uv --directory apps/api run alembic upgrade head && (api + worker) && scripts/smoke-generation … && uv --directory apps/api run pytest -q && scripts/check-standards` → 0 / 108 passed / ok | 2026-09-13 03:27 |
| Spec 006 Explore design pack: `/` signed-out landing page — hero, 5 tool cards, and the 12-preset gallery grouped camera → cinematic → dynamic with "Recreate" deep-linking to `/create/video?preset=<slug>` (the existing spec-004 `usePresetSelection` reader); no contract change; 4 disjoint briefs | `docs/specs/006-explore/{spec,design,tasks}.md`, `docs/tasks/T-006-{1..4}/brief.md` | `python3 -c "import json;print('\n'.join(sorted(json.load(open('packages/contracts/openapi.json'))['paths'])))" && ls docs/tasks \| grep T-006 && scripts/check-standards` → 10 paths unchanged (contract `sha256 17addc27…`), T-006-0..4 present, 0 violations; 10/10 ACs mapped, 20 files each owned by exactly one task (full output in `docs/tasks/T-006-0/report.md`) | 2026-09-13 03:38 |
| Neon (hosted Postgres) wired + migrated: pooled `DATABASE_URL` read from the gitignored `.env.local` (env wiring only, no code change), `alembic_version=0003` with the 12 presets seeded on Neon, `/api/health` → `{"status":"ok","database":"ok"}` HTTP 200; Modal CLI token authenticates (`modal app list` exit 0); **R2 is the remaining blocker** for the Modal spike (`modal secret list` empty) | `apps/api/app/settings.py` (read the DSN), `.env.local` (gitignored, untracked); evidence in `docs/tasks/T-002-4/report.md` | `uv --directory apps/api run alembic upgrade head && uv --directory apps/api run uvicorn app.main:app --port 8012 & curl -s /api/health` → 200 `{"status":"ok","database":"ok"}`; `alembic_version=0003`, `preset_count=12`, `host_is_neon=True`, `host_is_pooler=True` (full output in `docs/tasks/T-002-4/report.md`) | 2026-09-13 13:23 |
| Explore shared preset data + copy/helpers: `usePresets` moved to `api/presets.ts` and re-exported through `createVideoTypes.ts` (spec 004 unchanged); `exploreCopy`, `TOOL_CARDS`, `groupPresetsByCategory`, `recreateHref`, `presetTileStyles` with 10 new tests | `apps/web/src/api/presets.ts`, `apps/web/src/features/explore/{exploreCopy,toolCards,groupPresetsByCategory,recreateHref,presetTileStyles}.ts` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run test && npm --prefix apps/web run build && scripts/check-standards` → 53 passed (10 new), build ok, 0 violations (re-run by the orchestrator) | 2026-09-13 19:29 |
| Explore hero + tool cards: `ExploreHero` (one `h1`, subtitle, two `ButtonLink` CTAs) and `ToolCard`/`ToolCards` (5 cards from `TOOL_CARDS`, one link each, 1→2→5 grid) | `apps/web/src/features/explore/{ExploreHero,ToolCard,ToolCards}.tsx` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run build && scripts/check-standards` → all pass, 0 violations (re-run by the orchestrator) | 2026-09-13 19:35 |
| Explore effect gallery: `id="effects"` section, per-category `aria-labelledby` sections in camera → cinematic → dynamic order, cards with always-visible name/category/cost and an accessible "Recreate" deep link, plus loading skeleton / error+Retry / empty states | `apps/web/src/features/explore/{PresetGallery,PresetGalleryCard,PresetGalleryStates}.tsx` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run build && scripts/check-standards` → all pass, 0 violations (re-run by the orchestrator) | 2026-09-13 19:35 |
| Explore assembled as the index route: `/` renders hero → 5 tool cards → the grouped effect gallery from one `usePresets()` call; `HomePage` retired (no references remain) | `apps/web/src/features/explore/ExplorePage.tsx`, `apps/web/src/App.tsx` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run build && scripts/check-standards` → 89 modules built, 0 violations; signed-out `GET /api/v1/presets` → 200 with 12 presets; served bundle contains the Explore copy and no HomePage copy (re-run by the orchestrator) | 2026-09-13 19:40 |
| API: `GET /api/v1/jobs` Library list (owner-scoped, newest-first, `limit` 1–100 default 50, thumbnail poster→input→null, `preset_name` falls back to slug, cross-guest isolation — no existence leak) | `apps/api/app/repositories/jobs.py`, `apps/api/app/services/job_views.py`, `apps/api/app/routers/jobs.py`, `apps/api/tests/test_library_api.py` | ruff + mypy clean, 115 pytest passed, `export-openapi` byte-identical, check-standards 0 violations (reviewer re-run) | 2026-09-13 20:28 |
| Library web data: guest-session runner moved to `api/guestSession.ts` (behaviour unchanged; `createVideoTypes.ts` re-exports `GuestSessionOutcome`/`RunWithGuestSession`), `api/library.ts` (`useLibrary` over `GET /api/v1/jobs`), typed client regenerated (+77/−1) | `apps/web/src/api/{guestSession,library}.ts`, `apps/web/src/api/generated/schema.d.ts`, `apps/web/src/features/create-video/{createVideoTypes,CreateVideoPage}.ts` | `npm --prefix apps/web run lint && typecheck && test && build && scripts/check-standards` → 57 tests, build green, 0 violations (orchestrator re-run) | 2026-09-13 20:28 |
| Library copy + time helper: `libraryCopy.ts` (every design § Copy string, ASCII apostrophe) and `formatCreatedAt.ts` (UTC, normalises ICU "Sept"→"Sep") + test | `apps/web/src/features/library/{libraryCopy,formatCreatedAt,formatCreatedAt.test}.ts` | `npm --prefix apps/web run lint && typecheck && test && build && scripts/check-standards` → 57 tests (4 new), 0 violations | 2026-09-13 20:28 |
| Library page UI: `LibraryPage` (useLibrary once; title/subtitle → states → list → result panel, no child fetch), `LibraryList`/`LibraryItem` (one `<button>` per row, text status, `aria-current`, `openLabel`), `LibraryStates` (loading `aria-busy`/error+Retry/empty), `LibraryResultView` (video inline from the list item — no `/jobs/{id}` fetch — failed/missing variants), `?job=` selection | `apps/web/src/features/library/{LibraryPage,LibraryList,LibraryItem,LibraryStates,LibraryResultView,useLibraryJobParam}.tsx` | `npm --prefix apps/web run lint && typecheck && build && scripts/check-standards` → 89 modules, 0 violations; `npm run test` → 57 passed (orchestrator re-run) | 2026-09-13 20:36 |
| Library assembled: `/library` → `LibraryPage` (98 modules in the bundle); signed-out `GET /api/v1/jobs` → 401, `/library` → 200 with the shell; cross-checked T-005-1…4 reports against code (one stale `typecheck no-op` claim now superseded by `8eccbd7`) | `apps/web/src/App.tsx` | `npm --prefix apps/web run lint && typecheck && build && scripts/check-standards` → 98 modules, 0 violations; curl probes in `docs/tasks/T-005-5/report.md` (orchestrator re-run) | 2026-09-13 21:58 |
| Share public read: `GET /api/v1/public/jobs/{job_id}` → `PublicJobResponse` (exactly 7 fields, cookie-free, 200 for any existing job / 404 unknown / 422 malformed uuid); no owner/prompt/credit/asset/error leakage (asserted by exact-key-set + absent-string tests) | `apps/api/app/routers/share.py`, `apps/api/app/services/share_views.py`, `apps/api/tests/test_share_api.py` | ruff + mypy clean (30 files), 122 passed (7 new), openapi byte-identical, 0 violations (reviewer re-run; orchestrator re-run 121+1 known-flaky reaper, passes alone) | 2026-09-13 21:58 |
| Share web data + copy: `api/share.ts` (`usePublicJob` — 404→notFound, no request for undefined id, isMounted/requestId guards, no `features/**` import) and `features/share/shareCopy.ts` (design § Copy byte-exact); `schema.d.ts` regenerated (+83/−0, additive) | `apps/web/src/api/share.ts`, `apps/web/src/features/share/shareCopy.ts`, `apps/web/src/api/generated/schema.d.ts` | `npm --prefix apps/web run lint && typecheck && test && build && scripts/check-standards` → 57 tests, 0 violations | 2026-09-13 21:58 |
| Share OG HTML route: `GET /v/{job_id}` serves server-rendered HTML with escaped `<title>` + og/twitter meta before `</head>`, registered BEFORE the SPA catch-all, `include_in_schema=False`, fallback shell when `index.html` absent; og:image/og:video only when ready, twitter:card player vs summary_large_image | `apps/api/app/routers/share_page.py`, `apps/api/app/services/share_html.py`, `apps/api/app/main.py`, `apps/api/tests/test_share_page.py` | ruff + mypy clean (31 files), 128 passed (6 new), openapi byte-identical, 0 violations (orchestrator re-run); live `/v/<id>` no-cookie check in the report | 2026-09-13 22:10 |
| Share page UI: `SharePage` (`useParams` → `usePublicJob`, title set/restore), `ShareResult` (single `h1`, poster video `muted playsInline controls preload=metadata`, Download + Make-your-own), `ShareStates` (loading/error/notReady/notFound/failed) — no identity branch, no new fetch, all strings from `shareCopy` | `apps/web/src/features/share/{SharePage,ShareResult,ShareStates}.tsx` | `npm --prefix apps/web run lint && typecheck && build && scripts/check-standards` → 98 modules, 0 violations; `test` 57 passed | 2026-09-13 22:10 |
| Share assembled: `/v/:jobId` → `SharePage` (103 modules; the route renders inside `AppShell`, and a stranger's `useSession()` only probes `/me` — no guest bootstrap, AC-9 holds); public no-JS proof: `curl /v/<id>` (no cookie) returns `<title>Dolly In · Higgsfield</title>` + og:title/description/type/url + twitter:card, unknown uuid → 200 generic | `apps/web/src/App.tsx` | `npm --prefix apps/web run lint && typecheck && build && scripts/check-standards` → 103 modules, 0 violations; curl evidence in `docs/tasks/T-007-5/report.md` (orchestrator re-run) | 2026-09-13 22:20 |
| Fake top-up API: `POST /api/v1/credits/topup` → `{amount: 100, balance: N}` — lock → `TOPUP` ledger insert (`job_id` NULL) → sum → commit; repeatable; no cookie → 401 | `apps/api/app/routers/credits.py`, `apps/api/app/services/credits.py`, `apps/api/app/domain/credit_rules.py`, `apps/api/tests/test_credits_topup_api.py` | ruff + mypy clean (31 files), 133 passed (5 new), openapi byte-identical, 0 violations (orchestrator re-run 132+1 known-flaky reaper, passes alone) | 2026-09-13 22:37 |
| Credits web: `api/credits.ts` (`useCreditsPage` via the shared guest runner, no loading flash, last-known balance kept on failure) + `creditsCopy`/`CreditsBalanceCard`/`CreditsTopUpCard`/`CreditsPage`; `/credits` routed (108 modules) | `apps/web/src/api/credits.ts`, `apps/web/src/features/credits/*`, `apps/web/src/App.tsx`, `apps/web/src/api/generated/schema.d.ts` | `npm --prefix apps/web run lint && typecheck && test && build && scripts/check-standards` → 57 tests, 108 modules, 0 violations (orchestrator re-run); live 60→160→260 + SPA 200 in `docs/tasks/T-008-4/report.md` | 2026-09-13 22:37 |
| Image data + backend (spec 009): migration `0004` (job kind/params, nullable preset/input, `output_image`, `job_image`), image repositories with video-only Library/read filters, `ImageModelAdapter` port + a dependency-free deterministic PNG placeholder + unconfigured modal/openrouter adapters | `apps/api/migrations/versions/0004_image_jobs.py`, `apps/api/app/models/{job,asset,job_image}.py`, `apps/api/app/repositories/{jobs,image_jobs}.py`, `apps/api/app/adapters/{image_model_adapter,png_placeholder,placeholder_image_adapter,backend_selection}.py` | `docker compose up -d --wait db && alembic upgrade head && ruff && mypy && pytest -q && export-openapi && git diff --exit-code && check-standards` → 169 passed (23 new), mypy clean (40 files), contract byte-identical, 0 violations (orchestrator re-run) | 2026-09-13 23:20 |
| Image API + worker (spec 009): `image-options` public payload, one-transaction `create_image_job` (lock → idempotency → cost → HOLD → step → notify), owner read with ready `image_urls` + `backend`, and the worker kind-dispatch (`run_image_step` + `complete_image_step_success` SETTLE/`output_image`/`job_image`, failures on the generic RELEASE path) | `apps/api/app/services/{image_job_creation,image_job_views,image_options,image_generation_runs,image_step_completion}.py`, `apps/api/app/routers/image_jobs.py`, `apps/api/app/worker.py` | same chain → 169 passed, mypy clean, contract byte-identical, 0 violations (orchestrator re-run) | 2026-09-13 23:20 |
| Image-create web (spec 009): SSE watcher + `jobStatus` moved to `api/` (generic over job type, one implementation for video/image/future Library), shared credits hook, `api/{imageOptions,imageJobs}.ts`, and the six `features/image-create/` components (copy table, settings/cost helpers + tests, cost shown before submit, API-driven chips) | `apps/web/src/api/{jobStatus,jobStatusWatcher,credits,imageOptions,imageJobs}.ts`, `apps/web/src/features/image-create/**`, `apps/web/src/api/generated/schema.d.ts` | `npm --prefix apps/web run lint && typecheck && test && build && scripts/check-standards` → 60 tests (9 files), 109 modules, 0 violations (orchestrator re-run) | 2026-09-13 23:20 |
| Create-image slice live: `/create/image` routed (119 modules); `GET /image-options` (5 ratios / 2 qualities / max 4 / costs 10 and 15), guest 60 → 202 (cost 20, HOLD) → 40, same key → same id (one HOLD), succeeded with 2 presigned `image_urls` and SETTLE, the image downloads as a real PNG (`\x89PNG` + IHDR 640x360, 614840 B - not a renamed fixture), the video Library excludes it, `GET /jobs/{id}` 404 vs `GET /image-jobs/{id}` 200, and a 402 `{balance:0, required:60}` on a second guest's high/4 | `apps/web/src/App.tsx`, `apps/api/app/routers/image_jobs.py`, `apps/api/app/services/image_*.py` | `npm --prefix apps/web run lint && typecheck && build && scripts/check-standards` → 119 modules, 0 violations; full live trace in `docs/tasks/T-009-7/report.md` (orchestrator re-run) | 2026-09-14 00:05 |
| Image-job SSE fixed: `GET /api/v1/jobs/{image_id}/events` → 200 `text/event-stream` (was 404) — the stream route now authorises with an ownership-only lookup (`find_owned_job`, no kind filter) instead of the video-only `read_owned_job`; `GET /jobs/{image_id}` still 404s (AC-11) and image reads stay on `GET /image-jobs/{id}` | `apps/api/app/repositories/jobs.py`, `apps/api/app/routers/jobs.py`, `apps/api/tests/test_job_events_api.py` | ruff + mypy clean (40 files), 171 passed (2 new), openapi byte-identical, 0 violations (orchestrator re-run 170+1 known-flaky reaper, passes alone in 0.78 s); before/after curl in `docs/tasks/T-009-8/report.md` | 2026-09-14 00:20 |
| **LIVE on Railway + Neon (UNPOOLED) + R2**: the api service serves the SPA and the API from one origin — `GET /api/health` → `{"status":"ok","database":"ok"}`, `/api/v1/presets` → 12, `/` → 200 `text/html`, `/create/video` → 200 (deep links), `/api/v1/me` → 401 signed out, `/v/<unknown-uuid>` → 200 generic (OG route). Both services use the **unpooled** Neon DSN (pooled PgBouncer drops `LISTEN`/`NOTIFY` — see the fix row) | Railway project `higgsfield`, services `api` + `worker`, Neon, R2 bucket `higgsfield-assignment` | `curl` re-probe 2026-09-13 19:32 UTC: health 200 `{ok,ok}`, presets 12, `/` 200, `/create/video` 200, `/me` 401, `/v/<uuid>` 200 | 2026-09-13 19:32 |
| Test isolation: the lease-reaper flake is fixed — the test now clears the non-terminal step leftovers that the global reaper would otherwise re-queue (which the FIFO claim then returned instead of its own step) | `apps/api/tests/test_lease_reaper.py` | the full suite passed **twice in a row** (`171 passed` each) plus the ordered run; the test still fails if the reaper is broken (evidence in `docs/tasks/T-003-8/report.md`, orchestrator re-run) | 2026-09-13 18:50 |
| **LIVE end-to-end (spec 003 AC-1…AC-9) on the public URL**: guest (60 credits) → presigned R2 upload → job (idempotent, HOLD 20) → SSE `running -> succeeded` → real mp4 **1280×720, 120 frames, 5.000 s, h264** + poster, both in R2 → SETTLE → 4th job 402 `required=20` → another guest 404s on the job and its events | `scripts/smoke-generation`, Railway services `api` + `worker`, Neon (unpooled), R2 `higgsfield-assignment` | `SMOKE_STATUS_TIMEOUT=300 scripts/smoke-generation --base-url https://api-production-8afc.up.railway.app` → **PASS (9/9 steps)**, exit 0 | 2026-09-13 19:30 |
| **Fix: Railway services must use Neon's UNPOOLED endpoint.** The SSE broker and the worker's `pg_notify` need `LISTEN`/`NOTIFY`, and PgBouncer in transaction mode does not carry it — the pooled DSN delivered `running` and then silence forever, so every SSE client (and the 9-step smoke) hung while the job actually succeeded in ~13 s | Railway variables `DATABASE_URL` on both `api` and `worker` (set to the unpooled Neon DSN from the gitignored `.env.local`) | after the switch the same smoke delivered `running -> succeeded` and passed 9/9; before it, the identical run timed out at step 6 with `got ['running']` | 2026-09-13 19:30 |
| Spec 005 (Library) designed + contract published: `GET /api/v1/jobs` → `LibraryListResponse { items: LibraryItemResponse[] }` (owner-scoped, newest-first, `limit` 1–100 default 50) as a 501 stub; **no migration** (existing `ix_job_user_created` serves the query); 5 disjoint briefs | `docs/specs/005-library/{spec,design,tasks}.md`, `docs/tasks/T-005-{0..5}/brief.md`, `apps/api/app/schemas/jobs.py`, `apps/api/app/routers/jobs.py`, `packages/contracts/openapi.json` | `python3 -c openapi-paths && ls docs/tasks (filter T-005) && scripts/check-standards` → 10 paths incl. `/api/v1/jobs`, 5 briefs, 0 violations; openapi diff **+162/−0** and stable on re-export; stub live → 401 / 501 / 422 (full output in `docs/tasks/T-005-0/report.md`) | 2026-09-13 20:05 |
| `npm run typecheck` is now a real type gate: `tsc -b` follows the solution-style `tsconfig.json` references and checks `src` (192 files / 5059 TS lines) instead of zero files; a planted `TS2322` makes it exit 2 | `apps/web/package.json` (`typecheck` script) | `npm --prefix apps/web run typecheck && npm --prefix apps/web run build && scripts/check-standards` → typecheck exit 0 (was `Files: 0`, now `Files: 192`), build 89 modules, 0 violations; probe type error → exit 2 (full output in `docs/tasks/T-000-6/report.md`) | 2026-09-13 15:06 |
| Spec 007 (Share page) designed + its one new public route published: `GET /api/v1/public/jobs/{job_id}` → `PublicJobResponse` (no auth; no owner, prompt, credits, asset ids or error text) as a 501 stub, plus the `GET /v/{job_id}` OG-HTML plan; 5 briefs, no migration | `docs/specs/007-share/{spec,design,tasks}.md`, `docs/tasks/T-007-{0..5}/brief.md`, `apps/api/app/schemas/share.py`, `apps/api/app/routers/share.py`, `packages/contracts/openapi.json` | `python3 -c openapi-paths && ls docs/tasks (filter T-007) && scripts/check-standards` → 11 paths incl. `/api/v1/public/jobs/{job_id}` with **no security**, 5 briefs, 0 violations; openapi diff **+118/−0** and stable on re-export; stub live → 501 with no cookie, 422 for a bad uuid (full output in `docs/tasks/T-007-0/report.md`) | 2026-09-13 21:00 |
| Spec 008 (Credits + fake top-up) designed + contract published: `POST /api/v1/credits/topup` → `TopUpResponse {amount, balance}` as a 501 stub writing a pre-provisioned **`TOPUP`** ledger row of **+100** (a `GRANT` is impossible — the one-time partial unique index; D-013), **no migration**; `GET /credits` and every other path/schema byte-identical; 4 briefs in 4 waves | `docs/specs/008-credits/{spec,design,tasks}.md`, `docs/tasks/T-008-{0..4}/brief.md`, `apps/api/app/schemas/credits.py`, `apps/api/app/routers/credits.py`, `packages/contracts/openapi.json` | `python3 -c openapi-paths && ls docs/tasks (filter T-008) && scripts/check-standards` → 12 paths incl. `/api/v1/credits/topup`, T-008-0..4 present, 0 violations; contract diff **+77/−0** with 0 pre-existing entries changed; stub live → 401 no cookie / 501 with a guest cookie (full output in `docs/tasks/T-008-0/report.md`) | 2026-09-13 22:20 |
| Repo **public and fully pushed**: `https://github.com/Deepjyoti-Sarmah/Higgsfield-clone` returns 200 signed out; local `main` is in sync with `origin/main` (nothing unpushed) | `git remote origin`, GitHub | `curl -o /dev/null -w "%{http_code}" https://github.com/Deepjyoti-Sarmah/Higgsfield-clone` → 200; `git status -sb` → `main...origin/main` clean | 2026-09-13 19:32 |
| T-010 STATUS truth pass: the R2-BLOCKED, deploy-placeholder, gh-blocker and Neon-hang rows are retired; the LIVE row says UNPOOLED; NOT STARTED rewritten for specs 002–009 | `docs/STATUS.md`, `docs/PLAN.md`, `docs/WORKLOG.md`, `docs/tasks/T-010/` | `scripts/check-links && scripts/check-standards` → both `ok`, 0 broken / 0 violations (full output in `docs/tasks/T-010/report.md`) | 2026-09-13 19:32 |
| `apps/web/src/api/imageJobs.ts` split refactoring: extracted `imageJobHelpers.ts` (140 lines) to keep line count well under standards limit | `apps/web/src/api/{imageJobs,imageJobHelpers}.ts` | `npm --prefix apps/web run lint && npm --prefix apps/web run typecheck && npm --prefix apps/web run test && npm --prefix apps/web run build && scripts/check-standards` → 60 tests pass, 0 violations | 2026-09-14 03:33 |
| Verification report & walkthrough script written: live site slice verification documented in `docs/verification/VERIFICATION-REPORT.md`, 5-minute recording script in `docs/WALKTHROUGH.md` | `docs/verification/VERIFICATION-REPORT.md`, `docs/WALKTHROUGH.md`, `README.md`, `CAPTURE-TEST.md` | `scripts/check-links && scripts/check-standards` → 0 broken links, 0 violations | 2026-09-14 03:33 |
| Operability slice: `/api/health/deep` (5 timed checks, 503 semantics), JSON logging with job_id end to end, `scripts/load-test` 5/5 live on the free backend (p50 41.0 s / p95 58.1 s, $0), `docs/RUNBOOK.md` linked from README | `apps/api/app/{routers/health,services/system_health,schemas/health,logging_setup}.py`, `scripts/load-test` | `ruff + mypy + pytest -q` → 218 passed, `check-standards` ok, contract +100/−0, live load 5/5 (full output in `docs/tasks/T-034/report.md`) | 2026-09-14 01:42 |

## BROKEN / KNOWN ISSUES
| What | Where | Impact | Next step |
|---|---|---|---|
| First session log has a response but no prompt | `.agent-logs/2026-09-12_23-39-23_26732883-*.md` | cosmetic; hooks were installed mid-session | leave as is (append-only); explained in `CAPTURE-TEST.md` |
| Codex account out of quota until 2026-09-30 | `.agent-logs/*codex_T-000-4.md` (response is the quota error) | no real non-Claude agent answer yet; the wrapper itself works | use `openrouter:<model>` once `OPENROUTER_API_KEY` is set, or another CLI |
| First Codex run hung for 180s (stdin left open), so only its PROMPT was logged | same log file, entry 1 | cosmetic | fixed: stdin closed, `AGENT_RUN_TIMEOUT` logs timeouts as a response |
| `gemini`, `aider` not installed; `OPENROUTER_API_KEY` not set | — | only Claude Code and Codex are usable as agents right now | install/set when needed |
| `gh` CLI token invalid (keyring) — cosmetic only: the repo is already public and `main` is in sync with `origin/main` (see WORKS) | — | none: pushing/pulling works over git; only `gh` API calls fail | `gh auth login` if API automation is ever needed |
| **Modal LTX-2.5 spike GREEN on A10G**: real AI clip in R2 (`spikes/f7d0cf44-5a15-41d6-99b2-dd54bbbb8b8e.mp4`, 800,917 B, ftyp `isomiso2avc1`, served 200 as `video/mp4`); load 27.5 s + generate 339.8 s (121f 960×544, distilled sigmas, sequential offload); ~$0.20 total across 5 runs | `apps/gpu/ltx_spike.py`, Modal app `ap-fo98E8fzUL3Y6Y5xW4u3im` | `modal run apps/gpu/ltx_spike.py --image <picsum url> --prompt "slow dolly in"` → `r2_key` + public-URL byte probe (full output in `docs/tasks/T-012/report.md`) | 2026-09-13 21:30 |
| Real `ModalAdapter` live on H100: one clip through the adapter in **145.2 s** (938,665 B ftyp mp4 + 72 kB JPEG poster in `work_dir`); empty endpoint → `BackendNotConfiguredError`; timeout 600 s, 300 s lease survives via concurrent renewal (counted in-test); default still `local-motion`; contract byte-identical | `apps/api/app/adapters/modal_adapter.py`, `apps/gpu/ltx_spike.py`, `apps/api/tests/test_modal_adapter.py` | `ruff + mypy + pytest -q` → 183 passed, `check-standards` ok, paid adapter probe 145.2 s (full output in `docs/tasks/T-013/report.md`) | 2026-09-13 22:15 |
| Docker Hub `minio/mc` is unpullable here (`pull access denied`), so the `minio-init` compose service uses `quay.io/minio/mc:latest` (same publisher as the existing MinIO server image) | `docker-compose.yml` | cosmetic: the one-shot bucket-init service still creates `media` and sets it private | leave as is locally; swap back to `minio/mc` if the R2/deploy path needs the Docker Hub tag |
| `test_migrations.py` downgrades to `0001`, whose `0003` downgrade deletes presets; a surviving `job` row then fails on `job_preset_slug_fkey`. T-003-2's tests clean up after themselves, but T-003-4/T-003-5 API tests create jobs via `POST /jobs` | `apps/api/tests/test_migrations.py`, `apps/api/tests/conftest.py` | pytest can go red on the shared dev DB once the jobs-API tests land | add generation-table cleanup/truncate in conftest (or truncate in the migration test) before T-003-4/T-003-5 |
| `GET /api/v1/jobs/{id}` omits `credit_cost`, but `design.md` says polling returns "the same data" as create (which does return it) | `docs/specs/003-generation-core/design.md` (~line 43), `packages/contracts/openapi.json` (`JobResponse`) | contract + running code agree, so the design prose is the lower source; spec 004's poll fallback cannot show the cost | fix the design wording (or add `credit_cost` to the contract in a later spec) |
| AC-6's live 20 s `: ping` is proven only with a shortened interval in unit tests | `apps/api/tests/test_job_events_api.py` | the 20 s default is unproven end to end | cover with a long-running SSE check when convenient |
| T-003-7's brief cleanup `pkill -f "python -m app.worker"` also matches the shell running the verify chain, so it SIGTERMs its own run | `docs/tasks/T-003-7/brief.md` | noisy/aborted verify runs | use `pkill -f 'app\.worker'` or PID files; the orchestrator re-ran with a PID-safe variant |
| A live `app.worker` process races pytest on the shared dev DB (it claims freshly-created test jobs mid-assertion, e.g. `test_library_api.py::test_limit_truncates…` sees `queued` → `running` between two GETs) | `apps/api/tests/` (any job-creating test) | pytest can go red while a dev worker is running | stop the worker before `pytest -q`, or point tests at a dedicated DB | 2026-09-13 20:28 |
| **17 finished tasks have no prompt/response transcript** in `.agent-logs/` (T-000-6, T-005-1…5, T-006-2/3, T-007-1…5, T-008-1…4) — delegated DSH subagent runs with no auto-capture and no `scripts/agent-run` wrapper | `.agent-logs/`; those tasks' `report.md` | the assignment's capture requirement is only partly met, and a transcript can only be written by its own running session, so it cannot be reconstructed retroactively | stated plainly in `CAPTURE-TEST.md`, `README.md`, and `STATUS.md` | 2026-09-14 03:33 |
| The spec 009 Copy table has **no string for network / invalid submit errors** (only `generate.sessionError`) | `docs/specs/009-image-create/design.md` § Copy, `apps/web/src/features/image-create/` | the composer reuses `states.optionsError.body` for those two cases — exact for network, approximate for a 422 | add the two strings to the design + copy table in a follow-up | 2026-09-13 23:20 |
| Three files were edited outside the spec 009 Files tables and are documented rather than hidden: `apps/api/app/services/{share_views,generation_runs}.py` (nullable `preset_slug`/`input_asset_id` guards, approved by the orchestrator) and `apps/web/src/features/create-video/canvasPhase.test.ts` (one-line import fix after T-009-2 removed `isTerminalJobStatus`) | `docs/specs/009-image-create/design.md` § Files, `docs/specs/009-image-create/tasks.md` | the design/tasks file lists under-state what was touched | the file lists were updated in this commit | 2026-09-13 23:20 |
| Task C (final live Playwright verification: Explore/Create-video/Create-image/Library/Share/Credits screenshots) is **UNVERIFIED**: this machine's IP had already spent its 5/5 daily guest sessions before the walk started (confirmed via a rejected `POST /auth/guest` -> `429 {"limit":5,"used":5}`, no slot consumed) | `docs/verification/*.png` (stale, not overwritten this round) | the poster fix and the deploy (see WORKS) are real and confirmed independently, but the screenshots, the AI-video-badge/wall-clock check, and the fresh `VERIFICATION-REPORT.md` are not done | run the same Playwright walk from a different IP (e.g. mobile data) once the cap resets, or wait for the daily reset | 2026-09-14 |

## NOT STARTED
- Specs 002–009 are **all code-complete and committed** (walking skeleton, generation core, create video, library, explore, share `/v/{id}`, credits + fake top-up, create image with the placeholder PNG backend per D-014). Contract: 15 paths in `packages/contracts/openapi.json`. API: 171 pytest passed; web: 60 vitest passed; ruff + mypy --strict clean; `scripts/check-standards` 0 violations.
- Spec 003 is verified **live** 9/9 (`scripts/smoke-generation` vs the public URL, 2026-09-13 19:30): guest → R2 upload → job → SSE `running -> succeeded` (real 720p h264 mp4 + poster) → SETTLE → 402 → cross-guest 404.
- Verification notes documented in `docs/verification/VERIFICATION-REPORT.md`.
- Walkthrough recording script documented in `docs/WALKTHROUGH.md`.
